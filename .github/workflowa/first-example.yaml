name: My First Workflow

on: push

jobs:
  my-first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Welcome message
        run: echo "My first GitHub Actions job"

      - name: List files
        run: ls

      - name: Read README
        run: cat README.md